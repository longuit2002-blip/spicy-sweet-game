# Optional: source this file before `docker compose` so image tags stay out of `.env`.
# Example: set -a && source deploy.env && set +a && docker compose -f docker-compose.prod.yml up -d

WEB_IMAGE=ghcr.io/your-org/sweet-spicy-web:main
API_IMAGE=ghcr.io/your-org/sweet-spicy-api:main
