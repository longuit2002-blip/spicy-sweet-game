import { GAME_SERVER_TICK_INTERVAL_MS } from "@sweet-spicy/shared-types";

export const GAME_LOOP_TICK_INTERVAL_MS = GAME_SERVER_TICK_INTERVAL_MS;

/** Faster tick for server-driven bot plays / challenge actions (ms). */
export const BOT_DRIVER_TICK_MS = 450;
